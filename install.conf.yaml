- defaults:
    link:
      create: true
      relink: true
 
- clean: ['~']
 
- link:
    ~/.zshrc: zshrc
    ~/.config: config
 
- shell:
    - [git submodule update --init --recursive, Installing submodules]
